To-Do List App (Fixed Version)

This project is a simple To-Do List system written in **Python**.  
It started with faulty code provided as a class exercise, and our objectives was to fix the bugs, improve input validation, and practice collaboration using GitHub.


# What are the features? YOU CAN: #
- Add new tasks
- View all tasks
- Remove tasks by number (with validation)
- Input validation (no crashes on wrong input)
- Follows PEP8 coding style

## Setup Instructions ##

We used **Visual Studio Code (VS Code)** with its built-in terminal to run this project.  
Follow these steps to set it up on your computer:


1. Open **Visual Studio Code**.  
2. Open the integrated terminal:  
   - Menu: **View → Terminal**  
   - Or shortcut: `` Ctrl + ` `` (backtick)  
3. In the terminal, run:
   ```bash
   git clone https://github.com/your-username/todo-app-fix.git
2. Open the Project Folder
  - After cloning, open the folder in VS Code and open the terminal:
  ```bash
  = cd toDoApp-fix
  ```
3. Verify Python Installation
  - In the VS Code terminal, check if Python 3 is installed:
      ```bash
     python --version
4. Run the Application

Inside the VS Code terminal, run:
python toDoApp.py
